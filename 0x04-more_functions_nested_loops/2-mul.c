#include <stdio.h>
#include "main.h"

/**
 * mul - Multiplies two ints
 * @a: First operand
 * @b: Second operand
 * Return: mult
 */
int mul(int a, int b)
{
	int mult;

	mult = a * b;
	return (mult);
}
